# OPIc Personal Trainer 에이전트 지침

## 프론트엔드 작업
- impeccable 스킬들을 적절히 사용할 것.

## 테스트 범위와 가성비
- 테스트는 변경 전에 실패를 확인하는 TDD 순서를 따른다.
- 테스트 범위는 기능의 크기가 아니라 실패 비용과 계약 중요도로 결정한다.
- 인증·세션·CSRF·origin·rate limit·오류 봉투·데이터 보존·상태 전이는 회귀 비용이 높으므로 API 계약/통합 테스트를 우선한다.
- 순수 함수·라우트 파서·클라이언트 재시도 정책은 실행 비용이 낮으므로 작은 단위 테스트로 고정한다.
- 화면은 핵심 사용자 전이와 안전 문구만 테스트한다. 단순 CSS 조합, 모든 문구 변형, 같은 규칙을 반복하는 중복 테스트는 추가하지 않는다.
- 공통 미들웨어는 대표 보호 endpoint 하나로 실제 HTTP 흐름을 검증하고, 모든 endpoint에 동일한 테스트를 복제하지 않는다.
- 설정값만 확인하는 테스트는 실제 런타임 흐름을 보장하지 않음을 명시한다. 가능하면 실제 HTTP/세션 저장소/브라우저 흐름 검증을 보완한다.
- 테스트를 줄일 때는 보안·보존·상태 계약을 먼저 남기고, 중복·저비용 표현 테스트부터 제거한다.
- 단계 완료 보고에는 테스트 범위의 근거와 미검증한 실제 흐름을 숨기지 않고 기록한다.

## 검증 실행 순서와 병목 관리

- 구현 중에는 변경된 모듈의 관련 테스트만 실행하고, 기능 단위가 끝났을 때 해당 서버·웹 테스트를 실행한다.
- 최종 확인에서만 clean test/build와 전체 테스트를 실행한다. 같은 변경에 대해 무거운 전체 검증을 반복하지 않는다.
- 브라우저 검증 전에는 포트 점유, 서버 기동, API 연결, 필수 환경변수를 확인한다. 실제 API 흐름을 우선하며, 모킹을 사용하면 실제 검증과 구분해 기록한다.
- 검증 결과는 명령과 함께 기록하고, 환경 문제로 검증하지 못한 흐름을 성공한 것으로 간주하지 않는다.
- 병목이 발생하면 구현을 계속 확장하기 전에 원인을 분류한다: 문서 계약 확인, 의존성/환경 준비, 관련 테스트 실패, 전체 검증, 수동 검증.

## 작업 종료와 커밋

- 구현 작업이 완료되면 테스트와 필요한 검증을 실행한 뒤, `git status`와 diff를 확인한다.
- 커밋 전 변경 파일이 현재 작업 단계의 범위에만 해당하는지 확인하고, 관련 없는 기존 사용자 변경은 포함하지 않는다.
- 소스·로그·테스트 출력에 비밀값이나 개인 경로가 없는지 확인한 뒤 의도적인 단위로 커밋한다.
- 커밋 메시지는 Conventional Commits 형식을 따른다: `<type>(<scope>): <subject>`.
  - `type`은 다음 중 하나만 사용한다: `feat`(기능 추가), `fix`(버그 수정), `docs`(문서만 변경), `refactor`(동작 변화 없는 구조 변경), `test`(테스트 추가/수정), `chore`(빌드·설정·의존성 등 기타 잡무).
  - `scope`는 현재 작업 단계를 그대로 쓴다(예: `stage-03`). 여러 단계에 걸치거나 단계와 무관한 변경은 영역명을 scope로 쓴다(예: `deps`, `ci`).
  - `subject`는 영문 소문자로 시작하는 명령형 현재형으로 쓰고("add", "fix", "expose" 등), 끝에 마침표를 붙이지 않으며, 50자 이내로 무엇을 바꿨는지 요약한다. `Stage 01: ...`처럼 type/scope 없이 자유 서술하는 형식은 쓰지 않는다.
  - 예: `feat(stage-02): add authentication and app shell`, `fix(stage-03): expose session item identifiers`, `docs(stage-03): adopt jpa and lombok conventions`.
  - 커밋 하나가 여러 목적(기능+리팩터+문서 등)을 섞을 때는 커밋을 분리하거나, 가장 지배적인 목적을 type으로 선택하고 본문에 나머지를 bullet로 남긴다.
  - 본문(body)이 필요하면 제목 다음 빈 줄을 하나 두고 "무엇을"이 아니라 "왜"를 설명한다.
- 단계 작업이 끝나고 검증까지 통과하면 `master` 브랜치에 그대로 커밋한 뒤 원격 저장소로 push한다. 별도 브랜치를 만들거나 PR을 생성하지 않는다(1인 프로젝트로 PR 불필요).
